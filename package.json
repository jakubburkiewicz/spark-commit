{
  "name": "spark-commit",
  "displayName": "Spark Commit",
  "description": "AI-powered git commit message generator. Currently supports Claude CLI.",
  "version": "0.2.0",
  "publisher": "JakubBurkiewicz",
  "engines": {
    "vscode": "^1.85.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jakubburkiewicz/spark-commit.git"
  },
  "license": "MIT",
  "categories": [
    "SCM Providers"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "extensionKind": ["workspace"],
  "contributes": {
    "commands": [
      {
        "command": "spark-commit.generate",
        "title": "Spark Commit: Generate Commit Message",
        "icon": "media/sparkle.svg"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "spark-commit.generate",
          "when": "scmProvider == git",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Spark Commit",
      "properties": {
        "sparkCommit.cliPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to the Claude CLI binary. Leave empty to use `claude` from PATH. Useful when VS Code doesn't inherit PATH from your shell."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^6.0.3"
  }
}
